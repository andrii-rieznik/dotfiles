#!/usr/bin/env zsh

# ---  oh-my-zsh  --- #

export ZSH="$HOME/.oh-my-zsh"  # Path to your oh-my-zsh installation.


# ---  Homebrew  --- #

if [[ `uname` == "Darwin" ]]; then
  export HOMEBREW_NO_ANALYTICS=1  # prevent brew to collect statistic
fi


# ---  GPG key  --- #

export GPG_TTY=$(tty)
