### macOS ###
.DS_Store

### Homebrew ###
# - Lock files generated by `brew bundle dump` command
# - Brewfile variants, e.g. `Brewfile.local`, `Brewfile.private`, etc.
Brewfile.*

### Git ###
# gitconfig variants, e.g. `gitconfig.local`, `gitconfig.private`, etc.
gitconfig.*
